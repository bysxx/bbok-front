import { createQueryKeys } from '@libs/queryKeys';

export const CHECKLIST_KEYS = createQueryKeys('checklist');
export const FRIEND_KEYS = createQueryKeys('friend');
export const MEMBER_KEYS = createQueryKeys('member');
export const DIARY_KEYS = createQueryKeys('diary');
export const BOOKMARK_KEYS = createQueryKeys('bookmark');
